Chapters
========

0. Syntax: statements, expressions, data types & values
   (variables, if, while, numbers, strings, addition, concat, sub, mult, div, mod)
1. Input & Output
2. String length & indexing

3. Math
4. Functions

5. Arrays
6. Objects

7. Files
8. Network

N. Console user interfaces (ncurses, putchar, clear, sleep, getSize, events)


Next steps
==========

    * string: length & char. MAYBE: for loop, break, continue, +=, ++
    * clear
    * sleep

    * arrays: length & item
    * objects: props & dot syntax (& bracket syntax?)
    * functions?
    * for loops?

    * math: floor -> { trunc (handle negative), check if int, round, decimal remainder, ceil }
    * operator precedence
    * bitwise operators
    * hex, oct, binary

    * data types: number, string, boolean,  object, function,  null, undefined, nan
    * parseFloat, then make him re-create sbeve.inputNumber and only use his own
    * strings: charCodeAt


Ideas
=====

* maybe later: an element-lookup program that shows you info of an element you type in (eg mass, number, etc)


Extra
=====

* typeof


old scheduler
=============

    python3 -m src keep-alive
